#include<stdio.h>

int main()
{

float a,b,c,d,e;
  scanf("%f%f%f",&a,&b,&c);
  if((b+c)>a&&(c+a)>b&&(b+a)>c)
  {
     d=a+b+c;
      printf("Perimetro = %0.1f\n",d);

  }
  else
  {
      e =((a+b)/2)*c;
      printf("Area = %0.1f\n",e);
  }
 return 0;
}
