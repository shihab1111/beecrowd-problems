#include <stdio.h>
int main()
{
    int n,i,j;
    scanf("%d",&n);
int a[n];

for(i=0;i<n;i++)
{
    scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
    if(a[i]==1 || a[i]==2)
    {
        printf("0\n");
    }
    else 
    {
        j=(a[i]-1)/2;
        printf("%d\n",j);
    }
}
    return 0;
}
